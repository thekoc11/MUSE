# Core dependencies
numpy>=1.19.0
# torch>=1.7.0 - removed (will be installed via conda)
pandas>=1.1.0
scipy>=1.5.0

# Visualization
# plotly>=4.14.0 - removed (will be installed via conda)
dash>=2.0.0 
kaleido>=0.2.0 

# Natural Language Processing
fasttext>=0.9.2
gensim>=4.0.0
indic-transliteration>=2.0.0

# Machine Learning
# scikit-learn>=0.24.0 - removed (will be installed via conda)

# Web/Network
requests>=2.25.0

# Vector search
# faiss-cpu>=1.7.0  # Use faiss-gpu for GPU support
faiss-gpu>=1.7.0  # Use faiss-cpu if GPU support not available

# Others
tqdm>=4.50.0
pyyaml>=5.4.0

# MUSE-specific dependencies
# Add these based on specific needs of your MUSE implementation 